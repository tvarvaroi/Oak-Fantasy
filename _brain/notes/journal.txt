## 2026-05-29 — Pre-launch Security Checklist added

Living document creat de fondator pentru tracking comprehensive de
security tasks pre-launch. Locație: _brain/notes/SECURITY_CHECKLIST.md

Conținut (14 secțiuni):
1. API Keys rotation list (Resend, Supabase, Stripe, OAuth, SmartBill)
2. Environment variables audit (Production/Preview/Dev separation)
3. Git history secrets audit
4. Placeholder-uri legale (strategie lib/legal-info.ts centralized)
5. Domain & DNS setup (SPF, DKIM, DMARC pentru Resend)
6. Rate limiting (Upstash Redis pentru toate endpoint-urile public)
7. Stripe security (PCI, fraud protection, 3D Secure)
8. Database security (RLS audit, backup strategy)
9. Monitoring (Sentry, Speed Insights, uptime)
10. GDPR & legal compliance (DPA, data export, ANPC)
11. Performance audit (Lighthouse targets)
12. Final pre-launch checklist (Z-1 user journey test)
13. Post-launch monitoring (primele 7 zile)
14. Jurnal de rotații (tracking cronologic key rotations)

Update protocol: Claude Code update-ează document după fiecare sprint
cu task-uri sensibile descoperite. Special attention la Section 1
(API keys) și Section 14 (jurnal) la fiecare sprint end.

Pre-launch action: secțiunea TL;DR e checklist-ul minim înainte de a
conecta domain-ul oakfantasy.ro public.

NOTĂ Claude Code (commit-time): Section 1.1 (Resend API key) commit-uită
cu cheia REDACTATĂ pe valori. Repo e public (`tvarvaroi/Oak-Fantasy`),
GitHub Secret Scanning + Resend ar fi revocat automat cheia raw. Cheia
istorică `re_GnEqPV...` trebuie totuși rotated de fondator (e expusă în
chatul strategic anterior cu Claude). Workflow recomandat: rotire +
update env var Vercel + entry în Section 14.

## 2026-06-16 — Sprint 1 Task 1.1 (Cleanup) închis cu detour 2h

Task 1.1 original (Sprint 1 START):
- Delete tests/qa-screenshots/ (72 files, ~3MB)
- Rename `.from('waitlist')` → `.from('email_subscribers')` în lib/supabase.ts
- Verify cu smoke test ca anon pe staging
- Atomic commit + push

Plan: ~30 min. Realitate: ~3 ore din cauza unei fantome.

DETOUR — Spirală debug "42501 RLS regression":

Smoke test cu `.from('email_subscribers').insert({email}).select()` ca
anon returna 42501. Am presupus RLS regression după ALTER TABLE RENAME
(migration 20260522090009). Am pushed 5 migrations debug:
- 20260615180000 — "Allow anon subscribe" WITH CHECK true (ghost fix)
- 20260615190000 — drop+recreate "Anyone can subscribe"
- 20260615200000 — alter WITH CHECK true
- 20260615210000 — explicit grants + NOTICEs
- 20260615220000 — NOTIFY pgrst reload

Niciuna n-a rezolvat. Audit detaliat al pg_policies arăta state corect.
Service role INSERT funcţiona. Anon JWT verified pentru proiectul corect.

ROOT CAUSE (descoperit 2026-06-16 după restart proiect + retry):

`supabase-js .insert(entry).select()` cere PostgREST să returneze rândul
inserat printr-un SELECT post-INSERT. Anon NU are SELECT policy pe
email_subscribers — by design, doar service_role citeşte subscribers.
PostgREST mapează "INSERT succeeds but SELECT-back blocked" la cod
42501 misleading. Producţia foloseşte `.insert(entry)` fără chain
`.select()`. Producţia n-a fost niciodată broken.

CLEANUP final (canonical restore):

1. Migration nouă `20260616120000_restore_canonical_email_subscribers_policies.sql`:
   - Idempotent drop "Allow anon subscribe" + "Anyone can subscribe"
   - Recreate "Anyone can subscribe" cu canonical WITH CHECK email format
   - Defense-in-depth alongside Zod app-layer validation

2. `supabase migration repair --status reverted` pe toate 5 ghost migrations
   (180000, 190000, 200000, 210000, 220000). Tracking sincronizat cu disk.

3. Şters ghost fix `20260615180000_restore_anon_insert_email_subscribers.sql`
   din disk local. Repo 100% clean pentru prod fresh deploy.

4. Smoke test corect (scripts/smoke-canonical.mjs):
   - T1: production path `.insert()` fără `.select()` → PASS 201
   - T2: invalid email → 42501 (WITH CHECK enforcing) → PASS
   - Cleanup test rows via service_role

Schema final pe `juuozsjvuikdtjqhdylw`:
   policyname: "Anyone can subscribe"
   roles: {anon, authenticated}
   cmd: INSERT
   permissive: PERMISSIVE
   with_check: ((email IS NOT NULL) AND (length(email) > 3) AND (email LIKE '%@%'))

Reality clarification (added în brain + SECURITY_CHECKLIST):
- Un singur proiect Supabase real azi: `juuozsjvuikdtjqhdylw`
- "fgeubbsxucppypyjbdpg" era plan, NU implementare
- Site-ul Vercel rulează pe singurul proiect
- Pre-launch: create proiect prod nou, apply canonical migrations clean

Lessons recorded în `_brain/notes/gotchas.md`:
1. Single Supabase project, not staging+prod (reality clarification)
2. Supabase Free Plan auto-pause + PostgREST cache post-restore
3. RLS policies NU se migrează automat la ALTER TABLE RENAME
4. `.insert().select()` cu RLS — 42501 e misleading ← CRITICAL gotcha

Section 8 SECURITY_CHECKLIST expandat cu:
- 8.4 Supabase plan decision (Free vs Pro pre-launch)
- 8.5 Create prod project workflow + canonical state verification
- 8.6 RLS smoke test per tabel pre-launch + correct INSERT pattern

Time: detour 2h, cleanup ~30 min, brain docs ~30 min. Total ~3h
vs plan 30 min. Tradeoff: 4 gotchas durabile + workflow corect
documentat pentru future Supabase work pre-launch.

Next: Sprint 1 Task 1.2 (/contact page implementation).

## 2026-06-16 — Sprint 1 Task 1.2 (/contact) închis

Scope: pagină /contact bilingvă cu form + Resend integration test mode
+ admin notification + auto-reply (skipped în sandbox) + sidebar
LEGAL_INFO + Footer Contact link.

Decizii lock founder:
- D1 content-map inline (mirror About pattern, NOT messages/*.json)
- D2 install resend + react-email
- D3 prima rută API /api/contact
- D4 test mode logic + admin email TEST MODE warning banner
- D5 Footer refactor pentru page links
- D6 memory rate-limit cu HONEST threat model warning
- Q1-Q5 aprobate

Foundation creat:
- lib/legal-info.ts: single source of truth pentru date legale +
  contact. Toate [PLACEHOLDER] swap pre-launch via Vercel env.
- lib/schemas/contact.ts: Zod cu 5-subject enum, honeypot website,
  elapsedMs timing-check optional, SUBJECT_LABELS bilingv.
- lib/rate-limit.ts: in-process Map cu WARNING explicit că Vercel
  serverless invalidează persistence — pre-launch BLOCKER Upstash.

Templates React Email (Cream/paper palette, Caudex/Lora):
- emails/ContactAdminEmail.tsx cu conditional TEST MODE banner naming
  user email + missing-domain reason
- emails/ContactAutoReplyEmail.tsx bilingv cu Caveat handwritten
  signature + LEGAL_INFO footer

API route app/api/contact/route.ts workflow:
  1. rate-limit per-IP (5/h memory)
  2. Zod validate -> 400 per-field errors
  3. honeypot drop silent (200)
  4. timing-check elapsedMs < 2000 -> silent drop
  5. Resend admin email mandatory
  6. Resend auto-reply skipped în test mode cu console.warn
  Env: RESEND_API_KEY (required), RESEND_FROM_EMAIL,
       RESEND_ADMIN_EMAIL (default tvarvaroi@gmail.com)

UI:
- app/[locale]/contact/page.tsx cu generateMetadata + ContactPage JSON-LD
- components/contact/content.ts (CONTACT_CONTENT[locale] bilingv)
- ContactHero: Caudex headline + Caveat handwritten -1deg
- ContactForm: react-hook-form + zodResolver, full box-border inputs
  (mirror EmailForm), label-caps deasupra, message counter, hidden
  honeypot, timing elapsedMs, per-field errors, success state
  personalizat cu name + small leaf SVG ornament
- ContactInfo: paper-aged card cu lucide icons + LEGAL_INFO + placeholder
  banner când isPlaceholderMode + Caveat signature
- ContactPage: lg:grid-cols-12 split (form 7 / info 5), stacked mobile
- lib/i18n-routes.ts: PATHNAMES adăugat contact (same slug ro/en)

Footer:
- Adăugat Contact link în navLinks (page link, locale-aware)
- Email cross-site unificat la LEGAL_INFO.contactEmail
  (default info@oakfantasy.ro)

SECURITY_CHECKLIST updates:
- §4.1 TODO email final founder decide pre-launch
- §6.3 timing-check defense layer
- §6.4 (new) HONEST threat model: memory rate-limit near-zero on Vercel
- §6.5 (new) Resend monthly alert + upgrade path

BLOCKER hit în timpul build smoke test (RESOLVED):
- Functions în CONTACT_CONTENT (successHeadline, errorRate) → server
  component → client prop pass crash (Next 14 RSC nu serializează fn).
- Build static prerender /ro/contact + /en/contact timeout după 3 tries.
- Fix: template strings cu {name}/{minutes} placeholders + String.replace
  client-side + plural { one, many } map.
- Gotcha durabilă scrisă: "Funcții în copy map → server→client component
  pasează crash la build" — regula generală: orice prop server→client
  trebuie să fie JSON-serializable.

Verificat:
- npm run typecheck GREEN
- npm run lint GREEN (0 warnings)
- npm run check:i18n GREEN (5 content maps now)
- npm run build SUCCESS (/[locale]/contact 28.9 kB ro+en SSG'd,
  /api/contact dynamic)

Hand-off manual către founder:
- Submit form pe Vercel preview cu tvarvaroi@gmail.com (Resend test
  mode acceptă doar acel email)
- Verifică admin notification primit cu TEST MODE warning banner
- Verifică log Vercel: "[contact] TEST MODE: auto-reply to ... skipped"
- Pre-launch: setup oakfantasy.ro domain Resend + flip ON auto-reply

8 commits atomic, Vercel deploy automat la push.

Next: Sprint 1 Task 1.3 (pagini legale: termeni + confidentialitate + retur)
sau Task 1.4 (homepage updates) — așteptăm direcție founder.

## 2026-06-18 — Sprint 1 Task 1.3 (/termeni + /confidentialitate + /retur) închis

Scope: 3 pagini legale ca SCHELET STRUCTURAL — rute, metadata, JSON-LD,
layout, navigare, footer integration. NICIUN conținut juridic generat
de mine; toate secțiunile poartă același placeholder italic care anunță
"în pregătire". Avocatul completează pre-launch.

Decizii lock founder (Option A vs B/C):
- A locked: skeleton, NU draft starter copy (B blocked: risc juridic;
  C blocked: text final nedisponibil)
- A1 Commit order PATHNAMES + shared în Commit 1 (typecheck consistency)
- A2 Footer fine print row, NU coloana nouă (safer scope)
- A3 NO sticky TOC pe skeleton (Caudex/Lora prose 720px curat)
- A4 LegalCompanyInfo end-of-page (convenție juridică RO + mobile-first)
- A5 Text label "ACEST DOCUMENT ESTE ÎN PREGĂTIRE", NU emoji
- Skip /cookie-policy → deferred în SECURITY §10.1.b post-analytics decision

Foundation creat:
- lib/i18n-routes.ts: PATHNAMES expandat cu terms/privacy/returns
  (slug-uri diferite RO/EN, codified explicit)
- components/legal/ shared infrastructure:
    LegalLayout (wrapper full-page)
    LegalHero (Caudex headline + Caveat last-updated)
    LegalBanner (cream-warm "în pregătire" cu copper border)
    LegalSection (numbered heading 01..NN + default placeholder body
                  + sectionOverrides slot pentru statutory notes)
    LegalPlaceholder (acelaşi italic body pe TOATE secțiunile —
                     0% conținut juridic generat de mine)
    LegalCompanyInfo (paper-aged end-of-page card cu LEGAL_INFO pull)
    LegalStatutoryNote (citare statutorie cu eyebrow "Notă legală")
    content.ts (shared LegalContent / LegalSectionMeta types)

Content maps (titles bilingual, ids locale-agnostic English kebab-case
pentru deep-link stability):
- terms/content.ts: 14 secțiuni (parties, definitions, subject-of-contract,
                                  ordering, prices-payment, delivery,
                                  right-of-withdrawal, warranty,
                                  liability-force-majeure, dispute-resolution,
                                  personal-data-protection, amendments,
                                  acceptance, contact-info)
- privacy/content.ts: 14 secțiuni GDPR Articles 13-22 (data-controller,
                                  dpo, data-types, processing-purposes,
                                  legal-basis, recipients,
                                  international-transfer, retention,
                                  data-subject-rights, cookies, security,
                                  changes, gdpr-contact, complaint-right)
- returns/content.ts: 10 secțiuni OUG 34/2014 + RETURNS_STATUTORY export
                     pentru art. 16 lit. c citare în secțiunea exceptions

Pagini:
- app/[locale]/termeni/page.tsx (server component, metadata + WebPage
                                JSON-LD identic /despre + /contact contract)
- app/[locale]/confidentialitate/page.tsx
- app/[locale]/retur/page.tsx (foloseşte sectionOverrides pentru a
                              injecta LegalStatutoryNote sub
                              LegalPlaceholder în 'exceptions')

Footer:
- Fine print row sub copyright: 3 legal links cu bullet separators,
  page-level via localizedPath('terms'|'privacy'|'returns', locale)
- ANPC + SAL (ADR) + SOL (ODR) badges în copper accent label-caps,
  linkate la anpc.ro şi ec.europa.eu/consumers/odr

SECURITY_CHECKLIST updates:
- §4.1: toate 4 page slots (3 legale + contact) marcate [SLOT FILLED]
  cu hand-off clar pre-launch (env vars + lawyer copy = ready)
- §10.1.b (new): cookie policy page deferred until analytics lands;
  triggere documentate; LegalLayout infrastructure deja existentă

i18n checker GOTCHA discovered (workaround applied):
scripts/check-i18n.mjs foloseşte regex naiv pe array de obiecte —
multi-line entries cu id:/title: pe linii separate sunt detectate ca
top-level keys; single-line entries nu sunt. Asymmetric formatting
RO vs EN producea false positive "Missing EN key 'id'". Fix:
normalizat single-line consistent pentru toate array entries.
Considerăm îmbunătățire checker drept tehnical-debt minor (skip
known-array fields), nu blocker.

RSC blocker prevented (gotcha 2026-06-16 aplicată):
banner.bodyTemplate string cu placeholder `{contactEmail}` în loc de
funcție — substituire la render în LegalBanner client-side. Server→client
prop pass JSON-serializable.

Verificat:
- npm run typecheck GREEN
- npm run lint GREEN (0 warnings)
- npm run check:i18n GREEN (9 content maps now)
- npm run build SUCCESS (toate 3 pagini SSG ro+en, 844 B fiecare)

Visual regression test entries adăugate (8 noi: 2 contact catch-up + 6 legal).
Baseline regeneration HAND-OFF la founder — local .env.local pointează
la fgeubbsxucppypyjbdpg (proiect inexistent post-Task 1.1 reality
clarification), Playwright webServer ar fi rulat dev cu Supabase
broken pe homepage/catalog → corrupt batch. Founder rulează după
restore .env.local: npm run test:e2e:update -- --grep 'Visual regression'.

8 commits atomic:
1. feat(legal): foundation - PATHNAMES + shared components
2. feat(legal): /termeni + /terms skeleton (14 sections)
3. feat(legal): /confidentialitate + /privacy skeleton (14 sections GDPR)
4. feat(legal): /retur + /returns skeleton (10 sections + OUG 34/2014 cite)
5. feat(footer): add legal fine print row + ANPC/SAL-ADR/SOL-ODR badges
6. test(visual): add visual regression entries (8 new)
7. docs(security): legal slots filled + cookie-policy deferred TODO
8. docs(brain): Sprint 1 CLOSURE (acest entry)

═══════════════════════════════════════════════════════════════════
SPRINT 1 CLOSURE — Etapa 1 (Foundation) complete
═══════════════════════════════════════════════════════════════════

Period: 2026-06-15 → 2026-06-18 (~3 zile calendar, ~10h hands-on)

Tasks delivered:
- Task 1.1 ✓ Cleanup (qa-screenshots + waitlist→email_subscribers rename
                       + Supabase reality clarification)
- Task 1.2 ✓ /contact page cu Resend test-mode integration
                       + lib/legal-info.ts centralized placeholders
                       + lib/schemas/contact.ts Zod + bilingual labels
                       + lib/rate-limit.ts memory fallback cu HONEST warning
                       + emails/Contact*Email.tsx React Email templates
                       + Footer.tsx Contact link + email unify la LEGAL_INFO
- Task 1.3 ✓ Pagini legale skeleton (termeni, confidentialitate, retur)
                       + components/legal/ shared infrastructure
                       + Footer fine print row + ANPC/SAL/SOL badges

Stats Sprint 1 (from 307f7b4 baseline → c76267c HEAD pre-closure):
- 21 commits atomic
- 117 files changed
- 4481 insertions / 31 deletions (mostly new code, minimal churn)

Pre-launch BLOCKERS catalogued în SECURITY_CHECKLIST:
- §1.1 Resend API key rotation (cheia veche din chat strategic)
- §1 Setup Resend custom domain oakfantasy.ro + flip auto-reply ON
- §4.1 Populate NEXT_PUBLIC_COMPANY_* env vars în Vercel
- §6.4 Upstash Redis pentru rate-limit real (memory cache near-zero pe
       Vercel serverless)
- §6.3 Cloudflare Turnstile pe contact form ca defense layer 3
- §6.5 Resend monthly usage alert la 80%
- §8.4 Supabase Free vs Pro plan decision (auto-pause 7 zile)
- §8.5 Create prod project oakfantasy-prod + apply canonical migrations
- §8.6 RLS smoke test per tabel pre-launch
- §10.1.b /cookie-policy page (după analytics decision)
- Legal: lawyer review + fill components/legal/*/content.ts placeholders

Brain assets durabile create în Sprint 1:
- 4 noi gotchas (Task 1.1: 4) + 1 RSC functions gotcha (Task 1.2: 1)
  = 5 gotchas durabile totale. i18n-checker quirk on multi-line array
  entries noted dar nu scris ca gotchas separat (e checker-specific).
- _brain/notes/SECURITY_CHECKLIST.md: expandat substanțial cu §4.1
  slot tracking, §6.4-6.5 honest threat model, §8.4-8.6 Supabase
  pre-launch, §10.1.b cookie policy deferred, §14 rotații cu Resend
  decision.
- _brain/notes/journal.txt: 4 entries cronologice (Bootstrap,
  Pre-launch Security Checklist, Task 1.1 closure, Task 1.2 closure,
  Sprint 1 closure aici)

Next: Sprint 2 (Auth + Admin shell + CRUD Produse) sau revizuire
Sprint 1 deliverables cu founder înainte de Sprint 2 kick-off.

## 2026-06-18 — Task 1.4 (IA Foundation hotfix) închis

Trigger: post-Sprint-1 smoke test a găsit gap UX — /contact (+ paginile
legale) accesibile DOAR via Footer, niciun link primary în Navbar.
Founder a decis refactor IA complet (nu hotfix atomic) ca să acopere
și paginile Sprint 2/3 viitoare.

Audit a descoperit un al doilea bug ascuns: Footer NAVIGARE avea 4 din 5
link-uri ca #anchors (#workshop, #products, #story, #waitlist) — dead pe
TOATE paginile interior (există doar pe homepage).

Navbar refactor:
- Strip "Povestea noastră" (#poveste) + "Îngrijire" (#ingrijire) din primary
- Primary final = 4 route links curate: Atelier, Tocătoare, Despre, Contact
- NavLink type simplificat la route-only; anchor branches dead removed
- Section IDs rămân în homepage DOM (direct #anchor URLs încă merg)
- Active state neschimbat (1px copper underline)

Footer refactor 3→4 coloane:
- COL1 NAVIGARE: page links mirror Navbar (Tier 2), NU mai #anchors dead
- COL2 CONTACT: email mailto + phone + workshop addr + region + hours
  (din LEGAL_INFO, placeholder-aware) + Instagram (mutat aici, D6)
- COL3 CONT: Sprint 2 slot, "În curând"/"Coming soon" copper eyebrow +
  Caveat handwritten one-liner (-1deg), artisan voice nu enterprise (D1=B)
- COL4 COMPANIE: logo + brand + microtagline + brandDesc + operator/CUI
  din LEGAL_INFO + "Lucrăm cu drag" signature (mutat aici din Contact, D6)
- Helpers noi: FooterInfoRow (label-caps + value), FooterValue (dim+italic
  pentru [PLACEHOLDER])
- Mobile: stacked single column
- Fine print + ANPC/SAL/SOL row neschimbat

Decizii lock founder: D1=B (4 col cu Cont slot), D2 hide navbar pe /admin/*
(doar documentat), D3 logo→home always, D4 skip search, D5 strip anchors,
D6 Instagram→Contact + signature→Companie. Sub: active 1px, mobile stacked.

Brain: gotchas.md — "New Page Accessibility Checklist (IA 5-tier system)"
— Tier 1 Navbar / Tier 2 Footer NAVIGARE / Tier 3 fine print / Tier 4
hidden contextual / Tier 5 Sprint 4 expansion + checklist obligatoriu
la pagină nouă + D2 admin nav future decision documented.

Verificat: typecheck + lint + check:i18n + build GREEN (toate paginile
SSG ok, Navbar+Footer global change compilează).

Visual regression: Navbar + Footer = global → toate baselines vor diferi
în zonele top/bottom. HAND-OFF founder (același .env.local gap ca Task 1.3):
restore .env.local + npm run test:e2e:update -- --grep 'Visual regression'.
Spot-check așteptat: diff DOAR în Navbar/Footer zones, page bodies identice.

3 commits cod + brain (visual baselines = hand-off founder):
1. feat(nav): strip Povestea+Îngrijire, add Contact — 4 route links
2. refactor(footer): 4-column IA + Cont 'În curând' slot + Instagram→Contact
3. docs(brain): New Page Accessibility Checklist + 5-tier IA system

Sprint 1 FULLY CLOSED (cu hotfix IA). Ready Sprint 2 (Auth + Admin).

## 2026-06-18 — Sprint 2 Task 2.1 (Auth foundation wiring) închis

FINDING MAJOR la brainstorming: schema/RLS/trigger/functions auth EXISTĂ
DEJA din migrațiile 2026-05-22 (090001-090011, toate applied pe remote).
Premisa task-ului ("creem de la zero") obsoletă. ~85% deja livrat:
profiles (role CHECK customer/admin), orders (profile_id + guest, bani
integer, status enum 9 stări), order_items, addresses, products, inventory,
stock_movements, order_status_history, RLS complet, is_admin() SECURITY
DEFINER, guard_profile_role() anti-self-promotion, handle_new_user trigger,
generate_order_number (OF-YYYY-NNNN), reserve/release/fulfill_stock,
lib/supabase-server.ts, types/supabase.ts, @supabase/ssr installed.

Scope redus la 3 piese auth wiring genuin lipsă (decizii D1-D7 lock founder):
- lib/supabase-client.ts: createBrowserClient (auth browser client, session
  în cookies). Separat de lib/supabase.ts (waitlist anon) — D1.
- middleware.ts + lib/supabase-middleware.ts: session refresh compus cu
  i18n existing. refreshSession colectează cookies, applyCookies le pune pe
  fiecare return (next/redirect/rewrite) → Set-Cookie supraviețuiește
  redirect-urilor i18n. getUser() swallowed errors → anonymous OK. D4.
- lib/auth/get-user.ts (getUser → user+profile+role, isAdmin) +
  require-admin.ts (requireAdmin/requireUser cu redirect). D3.

STRATEGIC NOTE (founder): waitlist NU dispare — evoluează la newsletter
pre-launch. lib/supabase.ts (waitlist anon, neatins) coexistă cu
lib/supabase-client.ts (auth). Public-anon writes vs cookie-bound auth.

Smoke tests (scripts/smoke-auth.mjs, service-role + cleanup):
- T1 handle_new_user trigger → profile auto-created ✓
- T2 default role=customer ✓
- T3 guard_profile_role → self-promotion blocked (P0001) ✓
- cleanup: deleteUser → profile cascade ✓

Middleware runtime (prod server + curl): root→307 /ro, /en/despre→308
/en/about, /en/about→200 rewrite, /ro/contact→200, /ro→200. i18n intact.

Verificat: typecheck + lint + check:i18n + build GREEN. Middleware bundle
26.8kB→81.9kB (ssr client). Zero migrații noi. Types regen skip (D5).

HAND-OFF FOUNDER:
- Supabase Dashboard → Auth → Providers → Email → toggle "Confirm email"
  OFF (founder decision: signup fără email verification). Nu pot din cod
  (no supabase/config.toml local, settings în dashboard). D6.
- Promovare admin se face DUPĂ ce founder face signup (Task 2.2):
  UPDATE profiles SET role='admin' WHERE email='tvarvaroi@gmail.com';
  (din SQL editor — guard_profile_role exempt când auth.uid() IS NULL)

4 commits. Next: Task 2.2 (Customer auth pages /login, /inregistrare,
/parola-uitata, /reset-parola).

## 2026-06-18 — Sprint 2 Task 2.2 (Customer auth pages) închis

4 pagini auth + route handlers + logout minimal. Decizii lock D1-D6.

Routing (PATHNAMES): login (login/login), register (inregistrare/register),
forgotPassword (parola-uitata/forgot-password), resetPassword
(reset-parola/reset-password).

Foundation:
- lib/schemas/auth.ts: 4 Zod schemas (login, register cu confirm match +
  phone optional, forgotPassword, resetPassword cu confirm match). min 6
  parolă (Supabase default).
- components/auth/content.ts: AUTH_CONTENT[locale], RSC-safe (zero funcții),
  key parity ro/en. errors block anti-enumeration.
- components/auth/fields.tsx: shared primitives (inputBaseStyle,
  FieldLabel, FieldError, ServerError, SubmitButton) — consistency ContactForm.
- components/auth/AuthShell.tsx: Navbar + centered card (max 440px) + Footer
  + toggle (check #0 Task 1.5 — chrome global pe toate 4).

Forms (RHF + zodResolver, lib/supabase-client browser auth):
- LoginForm: signInWithPassword → redirect /[locale]. Anti-enumeration
  "Email sau parolă greșite" (nu dezvăluie care).
- RegisterForm: signUp(full_name metadata) → profile update full_name+phone
  (D3 A, RLS own update) → redirect home. Email-exists/weak-password mapping.
- ForgotPasswordForm: resetPasswordForEmail(redirectTo /auth/callback?next=
  reset) → MEREU success UI (anti-enumeration).
- ResetPasswordForm: getSession check (hasSession) → updateUser(password).
  Invalid/expired link → invalidLink message.

Route handlers:
- app/auth/callback/route.ts: exchangeCodeForSession → redirect next
  (open-redirect guard: doar relative /). Reused de reset + Google OAuth 2.3.
- app/auth/signout/route.ts: POST signOut → redirect /[locale] (locale din
  form body).

Navbar (D5 logout minimal): auth-state via browser client (getUser +
onAuthStateChange subscription). Când logat: "Ieși"/"Sign out" form POST la
/auth/signout (desktop lângă language toggle + mobile menu).

Pages (server): login + inregistrare cu redirect-away (getUser → redirect
home dacă logat, force-dynamic). forgot + reset SSG. robots noindex pe toate.

Verificat: typecheck + lint + check:i18n (10 maps) + build GREEN. Auth
pages First Load ~278kB (Navbar+Footer+supabase client).

BLOCKER (dashboard, nu cod): smoke-auth-flow.mjs → "Email signups are
disabled". Founder a făcut Confirm email OFF dar provider email signup /
"Allow new users to sign up" e încă OFF. Cod corect (eroare config
server-side). Vezi gotcha. Smoke live re-rulabil după ce founder enable.

Visual regression: 8 entries noi (4 pagini × 2 locale). Baselines =
hand-off founder.

HAND-OFF FOUNDER (prioritar — blochează acceptanța 2.2):
1. Supabase Dashboard → Auth → Providers → Email → ENABLE provider +
   "Allow new users to sign up" ON (+ Confirm email OFF deja făcut).
2. Auth → URL Configuration → Redirect URLs: adaugă
   http://localhost:3000/auth/callback + <vercel-url>/auth/callback
   (necesar reset password + OAuth Task 2.3).
3. DUPĂ signup tvarvaroi@gmail.com: SQL editor
   UPDATE profiles SET role='admin' WHERE email='tvarvaroi@gmail.com';
4. Regen visual baselines local + smoke-auth-flow.mjs (după gate-uri ON).

6 commits. Next: Task 2.3 (Google OAuth + /admin/login + middleware
protected routes).

## 2026-06-18 — Sprint 2 Task 2.2.1 (logout white screen hotfix) închis

Vezi gotcha "Middleware locale catch-all redirectează route handlers
non-locale". POST /auth/signout era 307-redirectat de middleware la
/ro/auth/signout (rută inexistentă) → white screen + #418/#423. Fix:
exclude /auth din matcher + guard în funcție. Verificat production build
curl. 1 commit (e30ae81).

## 2026-06-18 — Sprint 2 Task 2.3 (Admin protected routes) închis

Scope REDUS (Google OAuth deferred post-launch; foundation /auth/callback
ready). Doar admin auth + protected routes.

2 finding-uri arhitecturale (prevenite din lecțiile 2.2.1):
- /admin lovea catch-all locale (→ /ro/admin) → branch dedicat middleware
  (skip catch-all + păstrează session refresh pentru gate).
- /admin în afara [locale] → fără html/body → app/admin/layout.tsx propriu
  (html/body + fonts Caudex/Lora/Caveat, RO-only, NU gate).

Arhitectură (route group (protected) = single gate pentru toate paginile
admin viitoare):
- app/admin/layout.tsx — html/body + fonts (admin root, robots noindex)
- app/admin/not-found.tsx — 404 admin-styled (AdminCard)
- app/admin/login/page.tsx — PUBLIC, redirect /admin dacă deja admin
- app/admin/(protected)/layout.tsx — GATE: getUser() → notFound() dacă !admin
- app/admin/(protected)/page.tsx — dashboard placeholder "Bun venit, {nume}"
  + logout (form POST /auth/signout redirectTo=/admin/login)
- components/admin/AdminCard.tsx — centered card shared (login + 404)
- components/admin/AdminLoginForm.tsx — signInWithPassword → query role →
  admin? push /admin : signOut + "Acces interzis. Acest cont nu are
  drepturi de administrator."

Decizii lock D1-D6 + sub-decizie:
- D1 admin RO-only · D2 placeholder la 2.3 · D3 customer login admin →
  homepage · D4 notFound() idiomatic (NU middleware 404) · D5 /auth/signout
  cu redirectTo whitelisted (relative only, open-redirect guard) · D6 fonts
  replicate · sub: 404 hidden chiar și pt admin cu sesiune expirată.

Security layers: 3/4 active (L1 middleware + L2 RLS is_admin/policies +
L4 dedicated /admin/login izolat). L3 2FA TOTP → Task 2.6.

Verificat (curl, production build + dev):
- /admin nelogat → 404 ✓ (NU /ro/admin redirect)
- /admin/login → 200 ✓
- POST /auth/signout redirectTo=/admin/login → 303 /admin/login ✓
- open-redirect guard //evil.com → /ro ✓
- locale=en → /en ✓ · i18n intact (/en/despre → 308 /en/about) ✓
- typecheck + lint + check:i18n (warning benign admin RO hardcoded) + build GREEN
- /admin ƒ dynamic, /admin/login ƒ dynamic

DEBUGGING DETOUR (~45 min, lecție): smoke curl părea să arate redirectTo
ignorat (→/ro) + open-redirect leak (//evil.com→/evil.com). Root cause:
Git Bash MSYS path-conversion mutila argumentele curl (/admin/login →
C:/Program Files/Git/admin/login). Codul era corect tot timpul. Fix test:
MSYS_NO_PATHCONV=1. Gotcha scris. Lecție: rezultat "imposibil" cu sursa →
suspectează mediul de test (shell mangling / server stale) ÎNAINTE de cod;
debug header arată ce vede runtime-ul.

HAND-OFF FOUNDER — smoke manual browser (security verification):
1. Nelogat → /admin → 404
2. /admin/login → login page
3. Login tvarvaroi@gmail.com (admin) → redirect /admin dashboard
4. Cont customer test → /admin → 404
5. Customer → /admin/login → submit → "Acces interzis"
6. Admin logout → /admin/login → apoi /admin → 404

Commits + push. Next: Task 2.4 (Admin shell + /admin/produse CRUD) —
gate (protected) gata pentru drop-in pagini noi.

═══════════════════════════════════════════════════════════════════════
2026-06-22 — Task 2.4: Admin shell + /admin/produse CRUD (Sprint 2)
═══════════════════════════════════════════════════════════════════════

LIVRAT:
- Admin shell reutilizabil: components/admin/AdminShell.tsx (server, chrome)
  + AdminSidebar.tsx (client, usePathname active state). Sidebar 5 items:
  Panou + Produse active; Abonați/Profil/Comenzi disabled muted (D1). Topbar
  cu "Bun venit {nume}" + logout (form POST /auth/signout redirectTo=/admin/login).
  Randate în app/admin/(protected)/layout.tsx DUPĂ requireAdminOrNotFound()
  (gate la top → chrome-ul nu se construiește pentru non-admin; lecția RSC-leak 2.3).
- Storage: supabase/migrations/20260622090000_product_images_storage.sql —
  bucket 'product-images' (public read, 5MB, jpg/png/webp) + RLS pe storage.objects
  (admin insert/update/delete via is_admin(), public select). HAND-OFF: trebuie
  aplicată pe proiectul Supabase live (local nu poate aplica migrații).
- Data: lib/schemas/product.ts (Zod, RON in UI → bani în action), lib/admin/products.ts
  (fetchAllProducts = TOATE statusurile via admin RLS; fetchProductById),
  lib/admin/product-actions.ts (PRIMELE server actions: createProduct/updateProduct/
  toggleProductStatus, toate cu requireAdminOrNotFound + getServerSupabase RLS D4).
- Pagini: /admin/produse (listă, ProductTable client cu toggle optimist),
  /admin/produse/nou, /admin/produse/[id]/edit (ProductForm shared, toate câmpurile D8).
- ImageUpload.tsx: upload direct din browser (client autentificat) în Storage,
  hero + galerie, validare 5MB + format, șterge din Storage la remove (fără orfani).
- next.config.js: images.remotePatterns *.supabase.co/storage/v1/object/public/**.

GATES: typecheck ✓ · lint ✓ (0 warns) · check:i18n ✓ · build ✓ (doar warning-ul
pre-existent webpack cache about.module.css). Toate rutele /admin = dynamic (ƒ).

HAND-OFF FOUNDER:
1. Aplică migrația 20260622090000 pe Supabase (creează bucket + RLS).
2. Smoke: /admin/produse → vezi 10 seed-uri (toate draft) → adaugă produs cu
   imagine → editează → toggle ON (active) → verifică apare pe /tocatoare public
   → toggle OFF (archived) → dispare.
3. Commit + push (eu nu fac commit-uri).

NEXT: Task 2.5 (/admin/subscribers + CSV), apoi 2.6 (/admin/profile + 2FA).

--- 2026-06-22 (later) — Debug „imagini 404" (systematic-debugging) ---
Raport founder: URL cu /public/ → 404. Investigație empirică pe proiectul live
a DEMONTAT premisa: getPublicUrl() (deja folosit) produce /public/ = corect;
raw fetch /public/ = 200 stabil; next/image /public/ = 200; next/image FĂRĂ
/public/ = 400 (remotePatterns permite doar /public/**). Root cause real:
(1) next.config remotePatterns necommis pe serverul founder → next/image 400;
(2) 404 brut pe /public/ a fost CDN cache tranzitoriu (auto-vindecat).
DECIZIE: zero schimbare de cod URL; NU rula UPDATE SQL (ar sparge next/image).
Fix = deploy next.config existent + restart. Vezi gotcha 2026-06-22.

--- 2026-06-23 — Fix #418/#423 GLOBAL (systematic-debugging) ---
Founder a raportat /tocatoare + /admin/produse crash #418/#423, ipoteză „componentă
produs partajată". Reproducere Playwright pe PROD build a infirmat: eroarea pe TOATE
paginile (homepage/atelier/despre/tocatoare/contact/admin-login) la nivel #document.
Root cause: <SpeedInsights/> (v2.0.0 = <Suspense> intern) randat ca frate al <html>
în app/layout.tsx → Suspense boundary nehidratabil la rădăcină. Fix: mutat în <body>
în [locale]/layout.tsx; root layout = passthrough. Verificat clean pe 7 pagini.
NELEGAT de Task 2.4/produse — regresie de la bump speed-insights. Vezi gotcha 2026-06-23.

--- 2026-06-23 (later) — Verificare "#418 persistă pe prod" → NU persistă ---
Founder a raportat #418 încă pe producție după fix SpeedInsights (47b1424).
Re-investigație (systematic-debugging, metodologia validată):
- Local prod build (npm run build && start): CLEAN pe 7 pagini, normal + reduced-motion.
- Niciun service worker, niciun @vercel/analytics mis-plasat în cod.
- Găsit URL prod live: oak-fantasy.vercel.app. Playwright pageerror DIRECT pe prod:
  /ro, /en, /tocatoare(ro+en), /atelier, /despre, /contact, /termeni, /admin/login,
  + reduced-motion → TOATE clean, zero #418/#423.
- Dev mode (după fix): și el clean.
Concluzie: fix-ul SpeedInsights era COMPLET. Raportul founder = cache/propagare
la momentul testului (hard refresh golește browser cache, nu edge/SW cache).
Tip: pentru a confirma prod real, testează direct cu Playwright pageerror pe
oak-fantasy.vercel.app, nu doar în browserul personal (poate avea cache/extensii).

--- 2026-06-23 — Optimizare calitate imagine ProductCard ---
Blur cauzat de q75 default + sizes ușor mic. Fix: quality={90}, sizes aliniat la
layout real (400px desktop), next.config formats avif+webp. Verificat empiric:
optimizator servește 828×462 @ q90 corect (naturalWidth=400 era artefact Playwright).
Limită reziduală: sursa 16:9 landscape cropată pătrat rămâne ușor soft pe retina —
soluția reală = upload PĂTRAT 1:1 ≥1500×1500 (documentat în gotchas + recomandare founder).
Finding: fișier JPEG stocat ca .png (file.type greșit în ImageUpload) — nu strică, date murdare.

═══════════════════════════════════════════════════════════════════════
2026-06-23 — Task 2.5: /admin/subscribers + CSV export
═══════════════════════════════════════════════════════════════════════
LIVRAT:
- Migrație 20260623090000_email_subscribers_admin_read.sql — policy admin SELECT
  (FOR SELECT TO authenticated USING is_admin()). email_subscribers avea DOAR INSERT
  policy → admin via getServerSupabase primea 0 rânduri. HAND-OFF: aplică pe Supabase live.
- AdminSidebar: „Abonați" → active.
- app/admin/(protected)/subscribers/page.tsx — gate + fetch + count (+ activi dacă diferă) + Export CSV.
- components/admin/SubscribersTable.tsx (server component, fără hidratare) — email + dată RO + source + badge „Dezabonat" pe unsubscribed_at.
- lib/admin/subscribers.ts — fetchSubscribers() (RLS admin, [] pe eroare) + formatSubscriberDate (Intl ro-RO, Europe/Bucharest, null-safe).
- app/admin/(protected)/subscribers/export/route.ts — CSV download (Content-Disposition attachment),
  gate EXPLICIT în handler (layout NU gatează route handlers — lecția 2.3), BOM UTF-8 + CRLF (Excel),
  RFC-4180 escaping, filename subscribers-oak-fantasy-{YYYY-MM-DD}.csv. CSV: email+dată+language+source.

VERIFICAT: typecheck/lint/i18n/build green. Rute generate (ƒ). Non-admin → 404 pe page ȘI export
(body „Not found", zero leak). CSV logic validată offline (BOM, dată RO Bucharest UTC+3, escaping).
Calea autentificată (listă cu date) = founder smoke după aplicarea migrației + login admin.

NEXT: Task 2.6 (/admin/profile + 2FA TOTP — completează layer 3/4 securitate).
